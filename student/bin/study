인스턴스변수가 사라질때 : 더이상 나를 참조하는 개체가 없을 때 
(ex: new로 생성하고 해당 값이 null이 되었을때) 

클래스 메모리 로드가 될때 : 클래스가 해당프로그램 실행중에 언급될때 (프로그램이 종료될때까지 자원이 해제,삭제가 안됨) 

클래스변수의 초기화 시점 : 클래스가 로드 되었을때 
인스턴스 변수 초기화 : 인스턴스가 생성되었을때 

스택 : 후입선출 last in -> first out 
Q : 선입선출 first in -> last out 

JVM : 메서드영역 , 호출스택 , 힙 

this : 미래에사용하게 될 객체 주소값 

this 와 , super : this 는 "나"의 인스턴스 , super은 "조상"의 인스턴스

오버로딩 : 같은 이름을 가진 메서드가 서로 가진 다른 파라미터로 생성되었을때 한클래스 내에서 생성되었을때 

생성자 작성 규칙 : 클래스 블럭내에 생성 , 클래스명 = 생성자명 , 반환타입이 없음 , 파라미터괄호 및 타입 , 블럭을 이용한 구현부 작성 
생성자의 또다른 규칙 : 생성자 첫줄에는 this or super가 온다 new 연산자를 이용하여 생성자를 호출한다 , 호출하게되면 초기화가 진행 , 

생성자 여러개 오버로딩 가능 

기본생성자의 특징 : 따로 선언하지 않아도 자동으로 생성된다 단, 생성자가 한개도 없을때만 자동으로 만들어주고 생성자 한개이상이 만들어진다면 기본생성자 자동으로 생성하지 않음 

this. 와 this() 

this() : 내클래스에서 다른 곳에서 생성된 본인이외의 생성자 호출 
this. : 미래에 사용하게 될 객체 주소값 

초기화 블록 

import : import를 시켜주어 해당문구를 실행 / 하위패키지에 관한것은 임폴트x 
String 나 이런 것들은 자동 import
패키지는 계층구도보다는 수평구조 java util을 전부가져온다면 util만 가져오고 나머지 util.com... 이런것들은 가져오지 않음 / 폴더에 가깝다 / 클래스들의 묶음
패키지내의 패키지도 들어올 수 있음 

접근제한자 : 캡슐화를 담다앟고있는 것과 가까움 / 숨겨져서 안보이게끔 가시성에 대한 제한 = 접근제한자 / public과 private 중요 ! 
public : 어느 위치에서나 액세스 가능 
private : 클래스내에서만 액세스 가능 
지역변수는 접근제한자에 대해 언급할 수 없다 
메서드를 이용하여 접근 setter(입력값필요o 출력값x)  , getter(매서드의 반환 문법은 필드 타입을 일치시키면됨) / 
필드는 private로 주로 만든다 메서드와 클라스는 대부분 public / 내부에서 쓸 메서드는 private 로 사용한다 
상속 : 내것은 this / 조상것은 super 로 생성자 호출 
조상의 기본생성자가 없다면 자식은 명시적으로정의를 해주어야한다 
super() : 조상님 생성자 호출 
super. : 

메서드 재정의 : 

다형성 : 타입이 조상이고 자손의 인스턴스를 사용한다 

삭제 : 학번 일치제거 / 인덱스 1번삭제 - > 2,3번 인덱스가 1,2번이 되어야함 / 인덱스 삭제 말고 객체 삭제도 가능 / 정렬 / 입력값 중복제한 / 
팩토리얼 만들어보기 
dependency injection : 의존성 주입 

인스턴스메서드의 호출은 현재 실행중인 인스턴스를 기준으로 한다
추상클래스 : 블럭이 없다 , 추상메서드를 하나라도 가지고있다면 추상클래스 이다 

클래스의 관계를 어떻게 할 것 인가? 

인터페이스 :전부다 추상메서드임 /  다중상속이 가능하다 , 왜? 모든게 추상메서드 이기때문이고 구현은 나중에 할 것 이기때문이다
객체를 생성목적 혹은 클래스의 구현목적  / 명사를 수식한다 = 형용사 / 
인터페이스는 어떻게보면 추상클래스와 매우 비슷하다 

건물(공통조상) - gasO 건물 - 배럭 
		    - gasX 건물 - 팩토리
		    
		   = 배럭과 팩토리를 공통으로 묶어야한다 왜? 둘다 뜨는 성질이 있어야 하기 때문에 
		   공통으로 묶는것 -> 인터페이스(뜨게하는) : 추상메서드 이름 2개 뜨기,착지 
		   
과제 : 버블정렬을 이용하여 성적차순을 나타내기 / 삭제하기 / 학번 중복입력 방지 / 배열확장메서드: 10개의 배열이 꽉찾을때 배열을 늘려야할텐데 어느시점에 어떻게 호출시킬건지 
null값에 그전주소값을 담기만하고 안보여주면 됨 

시험 : 20문제 , 10 객관식 5 단답식(코드빈칸채우기) 5 서술 / 배열이전것  